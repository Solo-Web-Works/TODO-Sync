{
  "name": "todo-sync-action",
  "version": "1.0.0",
  "description": "GitHub Action to convert TODO comments into issues and summaries",
  "main": "index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}